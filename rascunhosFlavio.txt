const handleFieldChange = (field: string) => {
    return (value: string) => {
      const current = { ...values };
 
      current[field as keyof typeof values] = value;
 
      setValues(current);
 
   
    };
  };
 


  const [values, setValues] = useState<IFormState>({
    email: "",
    password: "",
  });



interface IFormState {
  email: string;
  password: string;
}


---------------------Text

Tarefas:
 
- montar página de formulário
--- email
--- senha
-  componente de botão:
- O botão vai receber um texto customizável
- Adicionar prop name nos componentes de input



EXEMPLO ON CLICK

import { useState } from 'react';

export default function Counter() {
  const [count, setCount] = useState(0);

  function handleClick() {
    setCount(count + 1);
  }

  return (
    <button onClick={handleClick}>
      You pressed me {count} times
    </button>
  );
}
